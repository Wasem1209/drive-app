import PhoneFrame from "../components/PhoneFrame";

export default function Driver() {
  return (
    <PhoneFrame>
      <p>
        This div is where you are to build on. Do not tamper with any other part of
        the code. Just add your div under this screen-content container.
      </p>
    </PhoneFrame>
  );
}